language: node_js
node_js:
  - '10'
cache: yarn
script:
  - yarn test
before_deploy:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" >> $HOME/.npmrc 2> /dev/null
deploy:
  provider: script
  script: "yarn release"
  skip_cleanup: true
  on:
    branch: master