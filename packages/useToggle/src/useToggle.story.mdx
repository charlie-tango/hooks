import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks'
import * as stories from './useToggle.story'

<Meta title="Hooks/useToggle" />

# useToggle

Simple `boolean` state toggler.

## API

```js
const toggledInitially = false
const [isToggled, toggle] = useToggle(toggledInitially)
```

## Examples

### Initial On

<Canvas>
  <Story story={stories.On} />
</Canvas>

### Initial Off

<Canvas>
  <Story story={stories.Off} />
</Canvas>
